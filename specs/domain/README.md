# Specs de dominio

`SPEC-R3-cadenetas.md` es la siguiente spec constructiva cerrada del proyecto heredado. Se conserva
completa porque contiene mediciones, alternativas descartadas y criterios cuantitativos.

Antes de implementarla se cierran `SPEC-001` y `SPEC-002`. Las specs R4–R8 se redactarán desde
`domain/ROADMAP-R1-R8.md` usando `templates/SPEC.md`.

