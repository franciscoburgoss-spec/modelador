# Specs de dominio

`SPEC-R3-cadenetas.md` es la primera spec constructiva completada sobre el proyecto migrado. Se
conserva completa porque contiene mediciones, alternativas descartadas y criterios cuantitativos.

Los cuatro cortes R3 están cerrados en `sessions/close-SPEC-R3-*.md`. La siguiente unidad debe
redactar `SPEC-R4` desde `domain/ROADMAP-R1-R8.md` usando `templates/SPEC.md`; R5–R8 siguen el mismo
proceso.
